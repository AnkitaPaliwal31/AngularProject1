import { SmartTableDirective } from './smart-table.directive';

describe('SmartTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartTableDirective();
    expect(directive).toBeTruthy();
  });
});
